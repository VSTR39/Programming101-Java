package montag;

import java.io.IOException;
import java.net.MalformedURLException;
import java.net.URL;
import java.util.ArrayList;
import java.util.HashSet;
import java.util.List;
import java.util.Queue;
import java.util.Set;
import java.util.concurrent.CopyOnWriteArrayList;
import java.util.concurrent.CopyOnWriteArraySet;
import java.util.concurrent.LinkedBlockingQueue;
import java.util.regex.Matcher;
import java.util.regex.Pattern;

//import org.apache.http.client.methods.HttpGet;

public class SimpleWebsiteCrawler {
	/*
	 * private CrawlerMemory mCrawlerMemory; private String mNeedle; private
	 * String mHaystack;
	 * 
	 * public SimpleWebsiteCrawler(String needle, String haystack) throws
	 * InterruptedException { this.mNeedle = needle; this.mHaystack = haystack;
	 * this.mCrawlerMemory.setLinks(new CopyOnWriteArrayList<String>());
	 * //this.mCrawlerMemory.getLinks().add(haystack);
	 * this.mCrawlerMemory.setVisited(new CopyOnWriteArraySet<String>()); }
	 * 
	 * public String getNeedle() { return mNeedle; }
	 * 
	 * public void setNeedle(String mNeedle) { this.mNeedle = mNeedle; }
	 * 
	 * public String getHaystack() { return mHaystack; }
	 * 
	 * public void setHaystack(String mHaystack) { this.mHaystack = mHaystack; }
	 * 
	 * private static List<String> getAllLinks(String content) {
	 * ArrayList<String> resultList = new ArrayList<>(); String regex =
	 * "<a.*?href=\"((?!javascript).*?)\".*?>"; Pattern pattern =
	 * Pattern.compile(regex); Matcher matcher = pattern.matcher(content); while
	 * (matcher.find()) { resultList.add(matcher.group(1)); } return resultList;
	 * }
	 * 
	 * public String crawl(URL haystack, String needle) throws IOException {
	 * boolean flag = false; String result = null; while(!flag){
	 * if(haystack.getHost().equals(mCrawlerMemory.getLinks().)) } }
	 * 
	 * public String getFirstIdentifier(URL url) { return
	 * (url.getHost().substring(url.getHost().indexOf('/') + 1,
	 * url.getHost().indexOf('.'))); }
	 * 
	 * public static void main(String[] args) throws IOException {
	 * 
	 * 
	 * }
	 */
}
